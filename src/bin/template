#![feature(let_chains)]

use std::collections::*;

use itertools::Itertools;
use regex::Regex;

use adventofrust::utils::*;

fn part1() -> String {
    let input = include_str!("../../input/%DAY%");

    String::new()
}

fn part2() -> String {
    let input = include_str!("../../input/%DAY%");

    String::new()
}

fn main() {
    println!("{}", part1());
    println!("{}", part2());
}
